Regenerate Flutter localizations after editing .arb files.

Run:
```
flutter gen-l10n
```

Then verify no errors were produced. This must be run any time `lib/l10n/app_en.arb` or `lib/l10n/app_fr.arb` is modified.
